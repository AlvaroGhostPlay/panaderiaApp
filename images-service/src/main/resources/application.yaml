spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://panaderia.test:9000

  application:
    name: ${SERVER_NAME}

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE}